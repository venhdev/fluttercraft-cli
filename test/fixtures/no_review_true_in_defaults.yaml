fluttercraft:
  build_defaults: &defaults
    platform: apk
    target: lib/main.dart
    no_review: true

  build:
    <<: *defaults

  environments:
    fvm:
      enabled: false
    shorebird:
      enabled: true
      no_confirm: true

  paths:
    output: dist
